plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  instance-state-name: running
compose:
  ansible_host: public_ip_address   # use public IP for SSH
keyed_groups:
  - key: tags.Role
    prefix: tag_Role_
