plugin: amazon.aws.aws_ec2
regions:
  - us-east-1   # Change to your EC2 region
filters:
  tag:Project: flask-app   # Change to match the tag you set
hostnames:
  - private-ip-address
keyed_groups:
  - key: tags.Name
    prefix: tag_Name
