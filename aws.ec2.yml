plugin: amazon.aws.aws_ec2
regions:
  - us-east-1               # Change to your AWS region
filters:
  instance-state-name: running
  "instance-id": "not:i-014872d8e09e56e20"   # Exclude Ansible control node
hostnames:
  - private-ip-address       # Use private IPs as hostnames
