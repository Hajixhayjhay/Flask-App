plugin: amazon.aws.aws_ec2
regions:
  - us-east-1          # Change to your AWS region
filters:
  tag:Role: flask       # For Flask servers
hostnames:
  - private-ip-address
keyed_groups:
  - key: tags.Role
    prefix: tag_Role
